# --- ROS 2 / colcon workspace (never commit build products) ---
build/
install/
log/

# --- Python (launch files, scripts, test tools) ---
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.venv/
venv/
env/

# --- C++ / CMake (in-package or stray in-source builds) ---
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake
*.a

# --- Editors & OS ---
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Optional: uncomment if you keep personal editor config local only
# .vscode/

# --- Large / machine-local artifacts (uncomment if you use them in-tree) ---
# *.bag
# *.mcap

# Vendored via: vcs import src < bot_sources.repos
src/csm/
src/ros2_laser_scan_matcher/

debug_logs/
