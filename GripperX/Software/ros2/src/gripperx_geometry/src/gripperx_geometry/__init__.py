"""Single source of truth for GripperX robot geometry.

See config/geometry.yaml for the declaration itself, inventory.py for the
extraction engine that reads consumers back, and generate.py for the derived
artefacts. This package has no runtime role: nothing on the robot imports it.
"""
